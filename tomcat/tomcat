				tomcat
				
1.下载与安装

	下载
	
		apache-tomcat.tar.gz
		
	安装
	
		将压缩包解压至/usr目录下,并在家目录下创建链接
			
			tar -zxf apache-tomcat.tar.gz
			mv apache-tomcat /usr/tomcat -rf
			ln -s /usr/tomcat/bin/startup.sh 	startup
			ln -s /usr/tomcat/bin/shutdown.sh 	shutdown
			
			