package Base;

/*
 * 集合
 * 	  (1)集合与数组：
 * 			a.长度区别：数组固定；集合可变。
 *   		b.内容区别：数组可以是基本数据类型，也可以是引用类型；集合只能是引用类型。
 *   		c.元素区别：数组只能存储同一类型；集合可以存储不同类型
 * */

public class JavaSet {
	
		/*
		 * List : Collection的子接口
		 * 		有序，可重复
		 * 		ArrayList  底层结构为数组，查询快，增删慢，线程不安全
		 * 		LinkedList 底层结构为链表，查询慢，增删快，线程不安全
		 * 		Vector 	   底层结构为数组，查询快，增删慢，线程安全
		 * 
		 * Set : Collection的子接口
		 * 		无序，唯一
		 * 		HashSet    底层结构是哈希表，哈希表底层依赖两个方法hashCode()和equals()
		 * 		TreeSet	   底层结构是红黑树
		 **/
	
	
		/*
		 * TreeMap:Map的实现类， 底层结构是红黑树
		 * */
		
	
		
}
